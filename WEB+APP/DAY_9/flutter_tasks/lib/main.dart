// Default entry point. Each task is a standalone app with its own main().
// This file runs Task 2 (Student Profile) as the default `flutter run` target.
// To run a specific task instead, use:  flutter run -t lib/taskN_*.dart
import 'task2_student_profile.dart' as task2;

void main() => task2.main();
